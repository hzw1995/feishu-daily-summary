name: 📅 飞书日报机器人

on:
  workflow_dispatch:
  schedule:
    - cron: '0 15 * * *'

jobs:
  summarize:
    runs-on: ubuntu-latest
    steps:
      - name: 发送测试消息到飞书
        env:
          FEISHU_WEBHOOK: ${{ secrets.FEISHU_WEBHOOK }}
        run: |
          MESSAGE="✅ GitHub Actions 测试成功！时间：$(date)"
          
          curl -X POST "$FEISHU_WEBHOOK" \
            -H 'Content-Type: application/json' \
            -d "{\"msg_type\":\"text\",\"content\":{\"text\":\"$MESSAGE\"}}"
